// import css from './NotFoundPage.module.css'

import { Section, Container } from 'shared';

const NotFoundPage = () => {
  return (
    <Section>
      <Container>Welcome to NotFoundPage</Container>
    </Section>
  );
};

export default NotFoundPage;

import { Section, Container } from 'shared';
import CalendarPagination from "../src/components/trakerPage/CalendarPagination/CalendarPagination";
const App = () => {
  // const { modalContent, setModalContent } = useModal();
  return (
    <Section>
      <Container>Hello world</Container>
      <CalendarPagination/>
    </Section>
  );
};
export default App;
